server:
  port: 8888
  
spring:
  application:
    name: config-server

#git远程配置
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/forezp/SpringcloudConfig/
          searchPaths: respo
          username:
          password:
      label: master
      
      